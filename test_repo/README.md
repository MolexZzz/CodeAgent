# TicketDesk

TicketDesk is a small internal support-ticket service. It is intentionally
implemented as a normal multi-file Python project with a repository layer,
business service, CLI formatting helpers, tests, and a product requirement
document.

## Quick start

```bash
python -m pytest -q
```

The maintenance brief for the next iteration is in `docs/requirements.md`.
